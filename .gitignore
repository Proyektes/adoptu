# Gradle
.gradle/
build/

# Local benchmark output (scripts/benchmark.sh)
scripts/benchmark-results/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.kotlin/
*.swp

# OS
.DS_Store
Thumbs.db

# Python (infra/lambda/*)
__pycache__/
*.pyc

# Logs
*.log
logs/

# Environment
.env
.env.local
*.env

# Build
*.jar
!gradle/wrapper/gradle-wrapper.jar
*.war
*.class

# Test results
/test-results/
/reports/

# Kotlin
*.class
*.log
*.ktds

# Kotlin/JS
# yarn.lock is committed for reproducible builds (see kotlinUpgradeYarnLock task)

# Distributions
build/distributions/*.zip
build/distributions/*.tar

# Temporary files
*.tmp
*.bak
*.orig

# Playwright
node_modules/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Claude Code worktrees (temporary, not for VCS)
.claude/worktrees/

# OpenWolf design QC screenshots (temporary, not for VCS)
.wolf/designqc-captures/

# OpenWolf live session-churn files (rewritten on nearly every tool call -
# tracking them in git causes constant stash/merge conflicts across
# sessions, branches, and worktrees; anatomy.md/buglog.json/cerebrum.md
# stay tracked since those are durable cross-session knowledge, not churn)
.wolf/hooks/_session.json
.wolf/memory.md
.wolf/token-ledger.json

# Playwright MCP debug snapshots (temporary, not for VCS)
.playwright-mcp/
.tokensave
